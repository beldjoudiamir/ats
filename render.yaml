services:
  - type: web
    name: ats-backend
    env: node
    plan: free
    buildCommand: cd backEnd && npm install
    startCommand: cd backEnd && npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 